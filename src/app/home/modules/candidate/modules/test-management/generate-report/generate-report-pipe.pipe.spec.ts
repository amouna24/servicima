import { GenerateReportPipePipe } from './generate-report-pipe.pipe';

describe('GenerateReportPipePipe', () => {
  it('create an instance', () => {
    const pipe = new GenerateReportPipePipe();
    expect(pipe).toBeTruthy();
  });
});
