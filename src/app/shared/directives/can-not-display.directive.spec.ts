import { CanNotDisplayDirective } from './can-not-display.directive';

describe('CanNotDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new CanNotDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
