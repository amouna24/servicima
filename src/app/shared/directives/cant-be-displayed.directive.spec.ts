import { CantBeDisplayedDirective } from './cant-be-displayed.directive';

describe('CantBeDisplayedDirective', () => {
  it('should create an instance', () => {
    const directive = new CantBeDisplayedDirective();
    expect(directive).toBeTruthy();
  });
});
