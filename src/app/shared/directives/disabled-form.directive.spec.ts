import { DisabledFormDirective } from './disabled-form.directive';

describe('DisabledFormDirective', () => {
  it('should create an instance', () => {
  /*  const directive = new DisabledFormDirective();
    expect(directive).toBeTruthy();*/
  });
});
